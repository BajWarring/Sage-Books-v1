import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sage.books',
  appName: 'Sage Books',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
